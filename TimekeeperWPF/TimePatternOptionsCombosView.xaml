<UserControl x:Class="TimekeeperWPF.TimePatternOptionsCombosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d" >
    <StackPanel Orientation="Horizontal">
        <ComboBox x:Name="C1" 
                      Visibility="{Binding Path=C1Visibility, FallbackValue=Visible}" 
                      ItemsSource="{Binding Path=C1Items}">
        </ComboBox>
        <ComboBox x:Name="C2" 
                      Visibility="{Binding Path=C2Visibility, FallbackValue=Collapsed}" 
                      ItemsSource="{Binding Path=C2Items}">
        </ComboBox>
        <ComboBox x:Name="C3" 
                      Visibility="{Binding Path=C3Visibility, FallbackValue=Collapsed}" 
                      ItemsSource="{Binding Path=C3Items}"/>
    </StackPanel>
</UserControl>

<UserControl x:Class="TimekeeperWPF.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             xmlns:cal="clr-namespace:TimekeeperWPF.Calendar"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400" Background="LimeGreen">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1">
            <ScrollViewer VerticalAlignment="Top" HorizontalAlignment="Left"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                <cal:Day Orientation="Vertical" ForceMaxScale="False" 
                         ShowTextMargin="True" >
                    <cal:CalendarObject Background="#7FFF6347" BorderBrush="Green" BorderThickness="1"/>
                    <cal:NowMarker/>
                </cal:Day>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>

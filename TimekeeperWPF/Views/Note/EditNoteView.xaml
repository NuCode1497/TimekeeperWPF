<UserControl x:Class="TimekeeperWPF.EditNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TimekeeperWPF"
             xmlns:wpftools="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Date &amp; Time"/>
        <wpftools:DateTimePicker Grid.Column="1" Grid.Row="0" Width="300" HorizontalAlignment="Left"
                                 VerticalContentAlignment="Center" 
                                 Text="{Binding Path=DateTime, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
        <Label Grid.Column="0" Grid.Row="1" Content="Note"/>
        <TextBox Grid.Column="1" Grid.Row="1" Width="300" HorizontalAlignment="Left"
                 TextWrapping="Wrap" MaxLength="160"
                 Text="{Binding Path=Text, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Column="0" Grid.Row="2" Content="Task Type" />
        <ComboBox Grid.Column="1" Grid.Row="2" Width="100" HorizontalAlignment="Left"
                  DisplayMemberPath="Name"
                  IsSynchronizedWithCurrentItem="True"
                  SelectedItem="{Binding Path=TaskType, Mode=TwoWay}"
                  ItemsSource="{Binding Path=DataContext.NoteTypesView, RelativeSource={RelativeSource AncestorLevel=2, AncestorType=Grid}}" />
        <local:LabelSelectorView Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3"
                                 DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorLevel=2, AncestorType=Grid}}"/>
    </Grid>
</UserControl>

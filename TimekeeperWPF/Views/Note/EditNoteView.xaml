<UserControl x:Class="TimekeeperWPF.EditNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Date &amp; Time"/>
        <TextBox Grid.Column="1" Grid.Row="0" Margin="2" Height="26" 
                 VerticalContentAlignment="Center" 
                 Text="{Binding Path=DateTime, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Column="0" Grid.Row="1" Content="Note"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="2" MinHeight="26"
                 TextWrapping="Wrap"
                 Text="{Binding Path=Text, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" MaxLines="2" MaxLength="160" />
        <Label Grid.Column="0" Grid.Row="2" Content="Task Type"/>
        <ComboBox Grid.Column="1" Grid.Row="2" 
                  DisplayMemberPath="Name"
                  IsSynchronizedWithCurrentItem="True"
                  SelectedItem="{Binding Path=TaskType, Mode=TwoWay}"
                  ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type Grid}, AncestorLevel=2}, Path=DataContext.NoteTypesView}" />
        <Label Grid.Column="0" Grid.Row="3" Content="Labels"/>
        <StackPanel Grid.Column="1" Grid.Row="3" x:Name="LabelsStackPanel" Orientation="Horizontal">
            <ItemsControl ItemsSource="{Binding Path=Labels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:LabelView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>

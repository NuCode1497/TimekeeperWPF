<UserControl x:Class="TimekeeperWPF.EditNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel x:Name="NoteEditPanel">
        <Label Content="Date &amp; Time"/>
        <TextBox x:Name="NoteDateTextBox" Margin="5" Height="26" 
                 VerticalContentAlignment="Center" 
                 Text="{Binding Path=NoteDateTime, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Note"/>
        <TextBox x:Name="NoteTextBox" Margin="5" MaxHeight="100" MinHeight="52"
                 TextWrapping="Wrap" 
                 Text="{Binding Path=NoteText, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>

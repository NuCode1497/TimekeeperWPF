<UserControl x:Class="TimekeeperWPF.EditNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Date &amp; Time"/>
        <TextBox Grid.Column="1" Grid.Row="0" Width="200" HorizontalAlignment="Left"
                 VerticalContentAlignment="Center" 
                 Text="{Binding Path=CurrentEditItem.DateTime, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Column="0" Grid.Row="1" Content="Note"/>
        <TextBox Grid.Column="1" Grid.Row="1" Width="200" HorizontalAlignment="Left"
                 TextWrapping="Wrap" MaxLength="160"
                 Text="{Binding Path=CurrentEditItem.Text, ValidatesOnDataErrors=True, 
            ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Column="0" Grid.Row="2" Content="Task Type" />
        <ComboBox Grid.Column="1" Grid.Row="2" Width="100" HorizontalAlignment="Left"
                  DisplayMemberPath="Name"
                  IsSynchronizedWithCurrentItem="True"
                  SelectedItem="{Binding Path=CurrentEditItem.TaskType, Mode=TwoWay}"
                  ItemsSource="{Binding Path=NoteTypesView}" />
        <local:LabelSelectorView Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3"/>
    </Grid>
</UserControl>

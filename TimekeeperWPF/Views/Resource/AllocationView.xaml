<UserControl x:Class="TimekeeperWPF.AllocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             xmlns:clr="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d">
    <ContentControl Template="{StaticResource EntityTemplate}" IsTabStop="False">
        <StackPanel Orientation="Horizontal">
            <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="2"
                       Text="{Binding Path=AllocationString, FallbackValue='qgyilkjQ^Test'}" 
                       FontSize="14" Foreground="{StaticResource BasicEntity.Foreground}" FontWeight="Bold"/>
            <Button Name="DeleteButton" IsTabStop="False"
                Style="{StaticResource EntityDeleteButtonStyle}" 
                CommandParameter="{Binding}"
                Command="{Binding Path=DataContext.DeleteAllocationCommand, 
            RelativeSource={RelativeSource AncestorType={x:Type Grid}}}"/>
        </StackPanel>
    </ContentControl>
</UserControl>

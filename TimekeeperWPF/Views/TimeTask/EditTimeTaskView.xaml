<UserControl x:Class="TimekeeperWPF.EditTimeTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d" 
             xmlns:wpftools="http://schemas.xceed.com/wpf/xaml/toolkit">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Name" Width="80"/>
            <TextBox Width="300" MaxLength="50" x:Name="NameTextBox"
                     Text="{Binding Path=Name, Mode=TwoWay, ValidatesOnDataErrors=True, 
                ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Description" Width="80"/>
            <TextBox Width="300" TextWrapping="Wrap" MaxLength="160" x:Name="DescriptionTextBox"
                     Text="{Binding Path=Description, Mode=TwoWay, ValidatesOnDataErrors=True, 
                ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Start" Width="80"/>
            <wpftools:DateTimePicker Width="300" x:Name="StartTextBox"
                                     Value="{Binding Path=Start, Mode=TwoWay, 
                UpdateSourceTrigger=PropertyChanged,
                ValidatesOnDataErrors=True,ValidatesOnExceptions=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="End" Width="80"/>
            <wpftools:DateTimePicker Width="300" x:Name="EndTextBox"
                                     Value="{Binding Path=End, Mode=TwoWay, 
                UpdateSourceTrigger=PropertyChanged,
                ValidatesOnDataErrors=True,ValidatesOnExceptions=True}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <local:TaskTypeSelectorView/>
                <!--<StackPanel Orientation="Horizontal">
                    <Label Content="Priority" Width="80" />
                    <wpftools:IntegerUpDown Width="100" Value="{Binding Path=Priority, Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>
                </StackPanel>-->
            </StackPanel>
            <!--<StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Dimension" Width="80" />
                    <wpftools:IntegerUpDown Width="100" Value="{Binding Path=Dimension, Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Power Level" Width="80"/>
                    <wpftools:IntegerUpDown Width="100" Value="{Binding Path=PowerLevel, Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"/>
                </StackPanel>
            </StackPanel>-->
        </StackPanel>
        <!--<WrapPanel MaxWidth="380" Margin="10,4,4,4" Background="Transparent" HorizontalAlignment="Left">
            <ToggleButton Content="AsksForCheckin" IsChecked="{Binding Path=AsksForCheckin}"/>
            <ToggleButton Content="AsksForReschedule" IsChecked="{Binding Path=AsksForReschedule}"/>
            <ToggleButton Content="CanReschedule" IsChecked="{Binding Path=CanReschedule}"/>
            <ToggleButton Content="RaiseOnReschedule" IsChecked="{Binding Path=RaiseOnReschedule}"/>
            <ToggleButton Content="CanBeEarly" IsChecked="{Binding Path=CanBeEarly}"/>
            <ToggleButton Content="CanBeLate" IsChecked="{Binding Path=CanBeLate}"/>
            <ToggleButton Content="CanBePushed" IsChecked="{Binding Path=CanBePushed}"/>
            <ToggleButton Content="CanInflate" IsChecked="{Binding Path=CanInflate}"/>
            <ToggleButton Content="CanDeflate" IsChecked="{Binding Path=CanDeflate}"/>
            <ToggleButton Content="CanFill" IsChecked="{Binding Path=CanFill}"/>
        </WrapPanel>-->
        <local:LabelSelectorView DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType=Grid}}"/>
        <local:IncludeSelectorView DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType=Grid}}"/>
        <!--<local:ExcludeSelectorView DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType=Grid}}"/>-->
    </StackPanel>
</UserControl>

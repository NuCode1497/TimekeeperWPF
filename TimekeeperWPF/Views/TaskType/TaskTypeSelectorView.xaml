<UserControl x:Class="TimekeeperWPF.TaskTypeSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Orientation="Horizontal">
        <Label Content="Task Type"  Width="80"/>
        <ListView Margin="5"
            ItemsSource="{Binding Path=DataContext.TaskTypesView, 
            RelativeSource={RelativeSource AncestorType=Grid}}" 
            SelectedItem="{Binding Path=TaskType, Mode=TwoWay}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel ItemWidth="45" Width="140"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </StackPanel>
</UserControl>

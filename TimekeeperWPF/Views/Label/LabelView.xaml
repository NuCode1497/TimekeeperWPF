<UserControl x:Class="TimekeeperWPF.LabelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d">
    <ContentControl Template="{StaticResource BasicEntityTemplate}" IsTabStop="False">
        <Button Name="DeleteButton" IsTabStop="False"
                Style="{StaticResource EntityDeleteButtonStyle}" 
                CommandParameter="{Binding}"
                Command="{Binding Path=DataContext.RemoveLabelCommand, 
            RelativeSource={RelativeSource AncestorType={x:Type Grid}, AncestorLevel=2}}"/>
    </ContentControl>
</UserControl>

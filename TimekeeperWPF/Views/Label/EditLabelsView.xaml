<UserControl x:Class="TimekeeperWPF.EditLabelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimekeeperWPF"
             mc:Ignorable="d" d:DesignWidth="200">
    <StackPanel Orientation="Horizontal">
        <ComboBox Grid.Column="1" Grid.Row="2" 
                  DisplayMemberPath="Name"
                  IsSynchronizedWithCurrentItem="True"
                  SelectedItem="{Binding Path=SelectedLabel, Mode=TwoWay}"
                  ItemsSource="{Binding Path=LabelsView}" />
        <Button Content="Add" Command="{Binding Path=AddLabelCommand}" 
                Visibility="{Binding Path=CanAddLabel, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <ItemsControl ItemsSource="{Binding Path=Labels}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:LabelView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
